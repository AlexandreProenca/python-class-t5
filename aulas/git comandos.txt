Comandos basicos do Git

Importante! os comandos devem ser digitados no diret�rio GIT

git clone          # baixa o repositorio da origem
git status
git logbb

# entra no diret�rio 
	git add    #adiciona o arquivo no stage
	git commit # salva um ponto no tempo
	git push   # envia as modifica��es
	git pull   # pega as atualiza��oes
